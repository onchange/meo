{
  "name": "meo-potential",
  "private": true,
  "scripts": {
    "dev": "npx serve",
    "sync-data": "cp data/municipalities.json data/census_businesses.json data/census_population.json data/averages.json public/data/",
    "deploy": "npm run sync-data && npx wrangler pages deploy public --project-name meo-potential --commit-dirty=true"
  }
}
